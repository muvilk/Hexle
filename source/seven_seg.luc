module seven_seg (
    input char[16],
    input digit[2],
    input segment[3],
    output out
  ) {

segment_selector seg;

  always {

    seg.char = char;
    seg.digit = digit;
    seg.segment = segment;
    out = seg.out;
  }
}
