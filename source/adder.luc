module adder (
    input a[32],
    input b[32],
    input alufn_signal[6],
    output out[32],
    output z,
    output v,
    output n
  ) {

  always {
    // default values to silence errors 
    out = 0;
    z = 0;
    v = 0;
    n = 0;
  }
}
